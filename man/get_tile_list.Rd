% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_tile_list.R
\name{get_tile_list}
\alias{get_tile_list}
\title{Identify GHSL tile IDs needed to cover spatial locations with a specified buffer}
\usage{
get_tile_list(
  locations,
  longitude_col = 1,
  latitude_col = 2,
  tile_schema,
  locations_crs = 4326,
  buffers_km = 10
)
}
\arguments{
\item{locations}{A data frame or matrix with longitude and latitude}

\item{longitude_col}{The column containing longitude in decimal degrees (character or numeric); defaults to column 1}

\item{latitude_col}{The column containing latitude in decimal degrees (character or numeric); defaults to column 2}

\item{tile_schema}{An \code{sf} object representing the GHSL tile grid schema, with a \code{tile_id} column.}

\item{locations_crs}{CRS code for the input coordinates (default is 4326)}

\item{buffers_km}{Numeric buffer radius in kilometers (default is 10).}
}
\value{
A list of character vectors, each containing the GHSL tile IDs intersecting the buffer around a location.
}
\description{
Given a set of geographic coordinates (e.g., sampling sites or institution locations),
this function identifies which GHSL population raster tiles intersect a buffer
of a specified radius around each point. It returns a list of tile ID vectors, one per input point.
}
\examples{
\dontrun{
# Example with mock coordinates and GHSL tile schema:
coords <- data.frame(lon = c(12.5, -122.3), lat = c(41.9, 37.8))
tile_list <- get_tile_list(coords, tile_schema = ghsl_tile_shapefile, buffers_km = 10)
}

}
